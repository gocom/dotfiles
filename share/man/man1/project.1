.\" Automatically generated by Pandoc 2.6
.\"
.TH "PROJECT" "1" "October 2018" "" ""
.hy
.SH NAME
.PP
project \[en] Initialize a project directory from a template
.SH SYNOPSIS
.PP
\f[B]project\f[R] [\f[I]options\f[R]]
.SH DESCRIPTION
.PP
Creates a new project directory from a template skeleton directory.
The program should be run in an empty directory that should host a new
project and answer the prompted questions.
.SH OPTIONS
.TP
.B \f[C]-h\f[R], \f[C]--help\f[R]
Print help.
.TP
.B \f[C]-v\f[R], \f[C]--version\f[R]
Print version number.
.TP
.B \f[C]-F\f[R], \f[C]--force\f[R]
Allow overwriting existing files.
.TP
.B \f[C]-q\f[R], \f[C]--quiet\f[R]
Non-interactive mode, does not prompt for answers, but instead either
uses defaults or given as options.
.TP
.B \f[C]-V\f[R], \f[C]-VV\f[R], \f[C]--verbose\f[R]
Verbose mode.
The more short \f[C]V\f[R] flags are used, the higher the verbosity
level is raised.
.TP
.B \f[C]-D\f[R], \f[C]--dry-run\f[R]
Dry-run without writing anything.
.TP
.B \f[C]--no-color\f[R]
Disable colors.
.TP
.B \f[C]--color\f[R]
Enable colors.
.TP
.B \f[C]-l\f[R], \f[C]--templates\f[R]
List available project template identifiers.
.TP
.B \f[C]-L\f[R], \f[C]--licenses\f[R]
List available license identifiers.
.SH TEMPLATES
.PP
Template skeletons are stored in \f[C]PROJECT_TEMPLATE\f[R], or
\f[C]\[ti]/.projects\f[R], directory, from where they are read and then
constructed using variables.
Variables are populated using interactive prompts, or from options given
when the program was ran.
Some values, such as author information, can be also collected from
external sources such as from \f[B]git\f[R].
.PP
The \f[C]PROJECT_TEMPLATE\f[R] directory should have the structure of:
.IP
.nf
\f[C]
$PROJECT_TEMPLATE/
  license/
    header/
      $license.license
    $license.license
template/
  $template/
    root/
    config.json
\f[R]
.fi
.SH ENVIRONMENT
.TP
.B \f[C]NO_COLOR\f[R]
Disables color output.
.TP
.B \f[C]PROJECT_TEMPLATE\f[R]
Path to directory where template skeletons are stored.
Defaults to \f[C]\[ti]/.projects\f[R] if not set.
.SH FILES
.PP
Depends on external programs: \f[C]node\f[R], \f[C]git\f[R]
.SH EXAMPLES
.PP
Create a new directory \f[C]test\f[R] and initialize project template in
it:
.IP
.nf
\f[C]
$ mkdir test && cd test
$ project
\f[R]
.fi
.SH AUTHORS
Jukka Svahn.
