.\" Automatically generated by Pandoc 2.6
.\"
.TH "FREENAME" "1" "October 2018" "" ""
.hy
.SH NAME
.PP
freename \[en] Print unique filename
.SH SYNOPSIS
.PP
\f[B]freename\f[R] [\f[I]options\f[R]] [\f[I]filename\f[R] \&...]
.SH DESCRIPTION
.PP
Checks whether the given \f[C]filename\f[R] exist and prints it to
\f[C]STDOUT\f[R] if it does not.
If the filename does exist, a new unused filename is generated by adding
a numerical suffix to the given \f[C]filename\f[R].
.PP
The suffix is appended before any file extension, unless the given
\f[C]filename\f[R] ends to a slash (\f[C]/\f[R]), where it\[cq]s added
to the end.
.SH OPTIONS
.TP
.B \f[C]-h\f[R], \f[C]--help\f[R]
Print help.
.TP
.B \f[C]-v\f[R], \f[C]--version\f[R]
Print version number.
.TP
.B \f[C]filename\f[R]
Filename to generate unique name out of.
If multiple filenames are given prints a list of filenames, one per
line.
.SH EXAMPLES
.PP
Generate unique out of already existing \f[C]file.txt\f[R]:
.IP
.nf
\f[C]
$ freename file.txt
> file-1.ext
\f[R]
.fi
.SH AUTHORS
Jukka Svahn.
