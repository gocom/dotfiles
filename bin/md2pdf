#!/usr/bin/env bash
"${DOTFILES_HOME:-$HOME/.dotfiles}/node_modules/.bin/md2pdf" \
  --config-file "${DOTFILES_HOME:-$HOME/.dotfiles}/home/.md2pdf.json" \
  --stylesheet "${DOTFILES_HOME:-$HOME/.dotfiles}/home/.md2pdf.css" \
  "$@"

#
